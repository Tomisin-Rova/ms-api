type Mutation {
    submitLiveVideo(
        id: ID!
    ): Result
    pingKYCService(
        message: String!
    ): Result
    createPhone(input: CreatePhoneInput!): Result
    verifyOtp(phone: String!, code: String!): Result
}
