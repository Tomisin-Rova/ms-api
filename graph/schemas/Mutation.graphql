type Mutation {
    getApplicantSDKToken(
        applicantId: String!
        applicationId: String!
    ): ApplicantSDKTokenResponse
    submitKYCApplication(
        applicantId: String!
    ): Result
}