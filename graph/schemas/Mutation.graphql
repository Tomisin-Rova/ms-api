type Mutation {
    submitKYCApplication(
        application_id: String!
    ): Result
    CreatePasscode(
        userId: String!,
        passcode: String!
    ): Result
    updatePersonBiodata(
        personId: String!,
        address: String!
        firstName: String!,
        lastName: String!
        dob: String!,
    ) : Result
    addReasonsForUsingRoava(
        personId: String!,
        reasons: String!
    ) : Result
}